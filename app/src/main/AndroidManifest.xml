<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a3rdwheel">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- Permissions for camera & gallery access for car photos -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.3rdWheel">
        <activity android:name=".HostPostDetailActivity" />
        <activity android:name=".userprofile.SellerProfileActivity" />
        <activity android:name=".userprofile.UserProfile" />
        <activity android:name=".HostCarDetailActivity" />
        <activity android:name=".HostNewPostActivity" />
        <activity android:name=".HostNewCarActivity" />
        <activity android:name=".HostActivity" />
        <activity android:name=".RentalActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>